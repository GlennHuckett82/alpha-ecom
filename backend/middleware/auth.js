// Placeholder — JWT auth middleware implemented in P20
// Verifies Bearer token and attaches req.user

const protect = (req, res, next) => {
  // TODO: implement in P20
  next();
};

module.exports = { protect };
