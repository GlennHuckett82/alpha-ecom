// Placeholder — implemented in P40/P41
// Config: API base URL, build hash for cache busting
const CONFIG = {
  API_BASE_URL: 'http://localhost:3000',
  BUILD_HASH: 'dev',
};

export default CONFIG;
